#!/bin/sh
###########

echo "-----------------------------------------------------------------------------------------"
echo "| LedgerCore is available on http://localhost:$NGINX_PORT by default.                             |"
echo "| Make sure your ports are mapped correctly (-p ${NGINX_PORT}:${NGINX_PORT}) when starting the container. |"
echo "-----------------------------------------------------------------------------------------"
